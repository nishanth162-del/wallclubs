spring.application.name=wallclubs

#spring.datasource.url=jdbc:postgresql://localhost:5432/wallclubs
#spring.datasource.username=postgres
#spring.datasource.password=root@123
#spring.jpa.hibernate.ddl-auto=update
#spring.thymeleaf.cache=false
# Local development (commented out - use these for local testing)
# spring.datasource.url=jdbc:postgresql://localhost:5432/wallclubs
# spring.datasource.username=postgres
# spring.datasource.password=your_local_password

# Render production (uncomment these for deployment)
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DATABASE_USERNAME}
spring.datasource.password=${DATABASE_PASSWORD}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.thymeleaf.cache=false   
server.port=${PORT:8080}      # Render sets PORT, defaults to 8080 locally
# Keep false for now, set to true later for performance